module <%= module_name %>
<% enums.sort_by { |e| e['name'] }.each do |enum| -%>

  module <%= MRubyBindings.to_rb_const(enum['name'].sub('enum ', '')) %>
<%   constants = enum['constants'].uniq
     constants.each do |constant| -%>
    <%= MRubyBindings.to_rb_const(constant['name']) %> = <%= constant['value'] %>
<%   end -%>
  end
<% end -%>
end
